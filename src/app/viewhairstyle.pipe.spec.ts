import { ViewhairstylePipe } from './viewhairstyle.pipe';

describe('ViewhairstylePipe', () => {
  it('create an instance', () => {
    const pipe = new ViewhairstylePipe();
    expect(pipe).toBeTruthy();
  });
});
